name: flutter_form_registry
description: A workaround to track some FormFields in the tree.
version: 0.3.2
homepage:

environment:
  sdk: ">=2.14.0 <2.17.0"
  flutter: ">=2.5.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
